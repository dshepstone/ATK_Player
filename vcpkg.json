{
  "$schema": "https://raw.githubusercontent.com/microsoft/vcpkg-tool/main/docs/vcpkg.schema.json",
  "name": "atk-player",
  "version": "0.1.0",
  "description": "Cross-platform animation playback and review application",
  "homepage": "https://github.com/dshepstone/ATK_Player",
  "license": "MIT",
  "dependencies": [],
  "features": {
    "ffmpeg": {
      "description": "Media decoding and export via FFmpeg (milestone M1). LGPL components only: GPL and nonfree encoders stay disabled so the application can ship under the terms recorded in docs/THIRD_PARTY_LICENSES.md.",
      "dependencies": [
        {
          "name": "ffmpeg",
          "default-features": false,
          "features": [
            "avcodec",
            "avformat",
            "avdevice",
            "swresample",
            "swscale"
          ]
        }
      ]
    }
  },
  "builtin-baseline": "",
  "overrides": [],
  "$comment": [
    "Qt is NOT listed here on purpose.",
    "Building Qt from source through vcpkg takes hours on Windows and produces",
    "a toolchain the official binaries already provide. Qt is installed",
    "separately (see docs/BUILDING.md) and found through CMAKE_PREFIX_PATH.",
    "",
    "FFmpeg is behind the optional 'ffmpeg' feature so Phase 0 configures with",
    "no dependency downloads at all, and vcpkg is not installed or required for",
    "Phase 0. Enable it in M1 with:",
    "    cmake --preset windows-debug -DCMAKE_TOOLCHAIN_FILE=%VCPKG_ROOT%/scripts/buildsystems/vcpkg.cmake -DVCPKG_MANIFEST_FEATURES=ffmpeg",
    "",
    "'builtin-baseline' must be filled in with a vcpkg commit SHA when the",
    "ffmpeg feature is first enabled, so dependency versions are reproducible."
  ]
}
